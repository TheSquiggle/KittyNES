# ROM-baked build artifacts containing copyrighted commercial ROM data --
# local-only, never pushed. (Public-domain/freeware test-ROM builds like
# nes_emulator_nestress.sb3 are fine to commit; anything baking in a
# commercial game's actual PRG/CHR bytes is not.)
progress/nes_emulator_smb_duckhunt.sb3

# Third-party reference .sb3 projects -- studied locally for technique only,
# never redistributed from this repo.
USE FOR REFERENCE ONLY AND ALSO GITIGNORE.sb3
progress/Famidash - Huge Man v1.2.8.sb3

# Framebuffer diagnostic dumps (regenerable via code/dump_frames.py)
progress/framedumps/
